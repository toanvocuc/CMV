node_modules/
.env
*.db
*.db-journal

/src/generated/prisma

# uploaded files (served at runtime, not committed — keep directory structure)
uploads/*
!uploads/.gitkeep
!uploads/videos/
uploads/videos/*
!uploads/videos/.gitkeep
!uploads/files/
uploads/files/*
!uploads/files/.gitkeep
